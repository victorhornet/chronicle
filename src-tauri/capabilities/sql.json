{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "sql-capability",
    "description": "Capability for the main window, sql",
    "windows": ["main"],
    "permissions": [
        "sql:allow-load",
        "sql:allow-execute",
        "sql:allow-select",
        "sql:allow-close"
    ]
}
